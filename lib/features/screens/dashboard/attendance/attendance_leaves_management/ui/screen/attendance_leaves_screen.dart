import 'package:flutter/material.dart';
import 'package:smart_cleaning_application/features/screens/dashboard/attendance/attendance_leaves_management/ui/widgets/attendance_leaves_body.dart';

class AttendanceLeavesScreen extends StatelessWidget {
  const AttendanceLeavesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return AttendanceLeavesBody();
  }
}
